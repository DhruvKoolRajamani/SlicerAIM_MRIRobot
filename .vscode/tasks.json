{
  "tasks": [
    {
      "label": "(Linux) build",
      "type": "shell",
      "command": "cmake --build ${workspaceFolder}/build --config Debug --target all -- -j 10 ../"
    },
    {
      "label": "(Win) build",
      "type": "shell",
      "command": "cmake --build ${workspaceFolder}/build --config Debug --target all -- /maxcpucount:8",
    }
  ],
  "version": "2.0.0"
}